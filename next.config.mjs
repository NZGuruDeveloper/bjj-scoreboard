/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export',
     // Optional: Change the output directory `out` -> `dist`
    //distDir: 'dist',
    output: "standalone",
};

export default nextConfig;
